# Changesets

Run `pnpm changeset` to record a change before merging. Versioning is fixed across
`@casoon/castwright`, `-player` and `-astro` — see `meta/decisions.md` ("Publishing").

https://github.com/changesets/changesets
